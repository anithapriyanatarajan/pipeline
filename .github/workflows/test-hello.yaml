name: Test Hello Workflow

"on":
  push:
    branches:
      - test-on

jobs:
  hello:
    name: Say Hello
    runs-on: ubuntu-latest

    steps:
      - name: Print hello message
        run: |
          echo "Hello from the test-on branch! 👋"
          echo "This is a simple test workflow."
          echo "Workflow completed successfully! ✅"
